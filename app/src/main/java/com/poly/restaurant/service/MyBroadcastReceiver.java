package com.poly.restaurant.service;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import com.poly.restaurant.utils.Constants;

public class MyBroadcastReceiver extends BroadcastReceiver {

    @Override
    public void onReceive(Context context, Intent intent) {
//        String idBill = intent.getStringExtra(Constants.EXTRA_ID_BILL_TO_BROADCAST);
//        Intent intentToDetail = new Intent(Constants.REQUEST_TO_ACTIVITY);
//        intentToDetail.putExtra(Constants.EXTRA_ID_BILL_TO_TABLE_DETAIL, idBill);
//        context.startActivity(intentToDetail);
    }

}
